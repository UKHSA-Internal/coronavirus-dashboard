import styled from "styled-components";
import type { ComponentType } from "react";


export const InlineLoading: ComponentType<*> = (() => {

    return styled.span`
        display: inline-block;
    `

})();
