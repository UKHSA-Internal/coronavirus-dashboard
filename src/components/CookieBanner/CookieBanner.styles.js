// @flow

import styled from 'styled-components';
import type { ComponentType } from 'react';

import type { StyleProps$Container } from './CookieBanner.types.js';

export const Container: ComponentType<StyleProps$Container> = (() => {
  return styled.div``;
})();
